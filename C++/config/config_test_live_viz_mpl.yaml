base_config: config_embryo_CellLumenFusion_VERIFIED_F085-120_temporalRepair_noTif_20260531.yaml

# Smoke config: same verified CellLumen fusion behavior, with the baked-in live
# visualizer auto-launching in headless 'mpl' mode (no display needed). Writes
# <output>/viz/playback_*.mp4 as frames complete.
simulation:
  export_frame_tiff: true   # write z-interpolated <frame>_real.tif for the viz

live_viz:
  enabled: true
  mode: 2d
  backend: mpl
